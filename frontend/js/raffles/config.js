/**
 * config.js - Централізована конфігурація для системи розіграшів
 * @version 1.0.0
 */

// Базова конфігурація системи
export const CONFIG = Object.freeze({
  // Налаштування API
  API: {
    BASE_URL: '/api',
    TIMEOUT: 30000,            // Таймаут запиту (30 секунд)
    CACHE_TTL: {
      ACTIVE_RAFFLES: 60000,   // Кеш активних розіграшів (1 хвилина)
      HISTORY: 300000,         // Кеш історії розіграшів (5 хвилин)
      USER_DATA: 120000,       // Кеш даних користувача (2 хвилини)
      STATISTICS: 600000       // Кеш статистики (10 хвилин)
    },
    RETRY_ATTEMPTS: 3,         // Кількість спроб повторення запиту
    RETRY_DELAY: 2000,         // Затримка між спробами (2 секунди)
    RATE_LIMIT_COOLDOWN: 60000 // Час очікування після перевищення ліміту запитів (1 хвилина)
  },

  // Налаштування оновлення даних
  REFRESH_INTERVALS: {
    ACTIVE_RAFFLES: 60000,     // Інтервал оновлення активних розіграшів (1 хвилина)
    HISTORY: 300000,           // Інтервал оновлення історії (5 хвилин)
    STATISTICS: 600000,        // Інтервал оновлення статистики (10 хвилин)
    USER_DATA: 120000          // Інтервал оновлення даних користувача (2 хвилини)
  },

  // Налаштування ініціалізації
  INITIALIZATION: {
    TIMEOUT: 10000,            // Максимальний час очікування ініціалізації модуля (10 секунд)
    RETRY_DELAY: 2000,         // Затримка перед повторною спробою ініціалізації (2 секунди)
    MODULE_INIT_ORDER: [       // Порядок ініціалізації модулів
      'api',                   // Спочатку API сервіс
      'uiComponents',          // Потім UI компоненти
      'active',                // Активні розіграші
      'history',               // Історія розіграшів
      'participation',         // Модуль участі
      'stats'                  // Статистика
    ]
  },

  // Налаштування відображення
  UI: {
    TOAST_DURATION: 3000,      // Тривалість показу повідомлень (3 секунди)
    ANIMATION_DURATION: 300,   // Тривалість анімацій (300 мс)
    DEFAULT_PAGE_SIZE: 10,     // Розмір сторінки для пагінації
    MODAL_CLOSE_DELAY: 300     // Затримка закриття модальних вікон (300 мс)
  },

  // Режим відлагодження
  DEBUG: false,

  // Шляхи до ресурсів
  ASSETS: {
    DEFAULT_IMAGE: '/assets/prize-poster.gif',
    TOKEN_ICON: '/assets/token-icon.png'
  },

  // Налаштування офлайн-режиму
  OFFLINE: {
    ENABLED: true,           // Увімкнення підтримки офлайн-режиму
    STORAGE_KEYS: {          // Ключі для локального сховища
      ACTIVE_RAFFLES: 'winix_active_raffles',
      HISTORY: 'winix_raffles_history',
      USER_DATA: 'winix_user_data',
      STATISTICS: 'winix_statistics'
    }
  }
});